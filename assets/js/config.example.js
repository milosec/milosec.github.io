/**
 * 🛡️ Sentinel: Security Configuration
 *
 * Instructions:
 * 1. Rename this file to 'config.js' (or copy it).
 * 2. Add your actual API keys below.
 * 3. Ensure 'assets/js/config.js' is in your .gitignore to prevent leaking secrets.
 */

window.appConfig = {
    // 🚨 CRITICAL: Do not commit the actual key to version control!
    googleMapsApiKey: 'YOUR_API_KEY_HERE'
};
